{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILEPATH=\"$TOOL_INPUT_FILE_PATH$TOOL_INPUT_file_path\"; if [ -n \"$FILEPATH\" ] && [ -n \"$WORKTREE_PATH\" ]; then case \"$FILEPATH\" in \"$WORKTREE_PATH\"/*) ;; *) echo 'BLOCK: Cannot edit files outside worktree boundary' >&2; exit 1;; esac; fi"
          }
        ]
      }
    ]
  }
}
